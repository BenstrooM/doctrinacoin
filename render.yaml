services:
  - type: web
    name: doctrinacoin
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn node:app --bind 0.0.0.0:10000 --timeout 120
    plan: free
    envVars:
      - key: PORT
        value: 10000